<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.atzuche.com.renterwz.mapper.RenterOrderWzIllegalPhotoMapper">
	<sql id="select_column">
		id,path,user_type,order_no,car_plate_num,serial_number,create_op,create_time,update_time,update_op,is_delete
	</sql>

	<resultMap id = "selectColumnMap" type="com.atzuche.order.renterwz.entity.RenterOrderWzIllegalPhotoEntity">
		<result property="id" column="id"/>
		<result property="path" column="path"/>
		<result property="userType" column="user_type"/>
		<result property="orderNo" column="order_no"/>
		<result property="carPlateNum" column="car_plate_num"/>
		<result property="serialNumber" column="serial_number"/>
		<result property="createOp" column="create_op"/>
		<result property="createTime" column="create_time"/>
		<result property="updateTime" column="update_time"/>
		<result property="updateOp" column="update_op"/>
		<result property="isDelete" column="is_delete"/>
	</resultMap>

	<insert id="saveRenterOrderWzIllegalPhoto" parameterType="com.atzuche.order.renterwz.entity.RenterOrderWzIllegalPhotoEntity" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
		insert into renter_order_wz_illegal_photo(
			path,
			user_type,
			order_no,
			car_plate_num,
			serial_number,
			create_op,
			is_delete,
			create_time
		) values (
			#{path},
			#{userType},
			#{orderNo},
			#{carPlateNum},
			#{serialNumber},
			#{createOp},
			0,
			#{createTime})
	</insert>

	<select id="queryRenterOrderWzIllegalPhotoById" resultType="com.atzuche.order.renterwz.entity.RenterOrderWzIllegalPhotoEntity">
		select <include refid="select_column" />
		from renter_order_wz_illegal_photo
		where id = #{id} and is_delete = 0
	</select>

	<select id="queryList" resultType="com.atzuche.order.renterwz.entity.RenterOrderWzIllegalPhotoEntity">
		select <include refid="select_column" />
		from renter_order_wz_illegal_photo
		where is_delete = 0
	</select>

	<select id="getMaxSerialNum" resultType="Integer">
		select
		max(serial_number) from renter_order_wz_illegal_photo where order_no = #{orderNo} and car_plate_num = #{carPlateNum}
		and user_type = #{userType}  and is_delete = 0
	</select>

	<select id="countPhoto" resultType="java.lang.Integer">
		select count(*)  from renter_order_wz_illegal_photo
		where order_no = #{orderNo} and path = #{path} and car_plate_num = #{carPlateNum}  and is_delete = 0
	</select>

	<select id="countIllegalPhoto" resultType="int">
		select count(*) from
		renter_order_wz_illegal_photo where order_no = #{orderNo}  and car_plate_num = #{carPlateNum}
		and user_type = #{userType}  and is_delete = 0
	</select>

	<select id="getIllegalPhotoBy" resultMap="selectColumnMap">
		select * from
		renter_order_wz_illegal_photo where order_no = #{orderNo} and
		user_type = #{userType} and serial_number = #{serialNumber} and car_plate_num = #{carPlateNum}  and is_delete = 0
	</select>

	<update id="update" parameterType="com.atzuche.order.renterwz.entity.RenterOrderWzIllegalPhotoEntity">
        update renter_order_wz_illegal_photo set
        path = #{path},
        update_time = #{updateTime},
        update_op = #{updateOp}
        where  order_no = #{orderNo} and car_plate_num = #{carPlateNum} and
		user_type = #{userType} and serial_number = #{serialNumber}  and is_delete = 0
    </update>

	<select id="queryIllegalPhotoByOrderNo" resultType="com.atzuche.order.renterwz.vo.PhotoPath">
		select user_type userType,path img,car_plate_num carNum
		from renter_order_wz_illegal_photo
		where order_no=#{orderNo} and car_plate_num = #{carPlateNum} and is_delete = 0
	</select>
</mapper>
